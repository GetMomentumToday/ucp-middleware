/**
 * @ucp-middleware/adapters
 *
 * Platform-specific adapter implementations.
 * Each adapter implements IShopAdapter from @ucp-middleware/core.
 *
 * Available adapters:
 *   - MagentoAdapter  (Magento 2.x REST API)
 *   - ShopwareAdapter (Shopware 6.x Store API)
 *   - ShopifyAdapter  (Shopify Storefront GraphQL)
 *
 * Adapters are planned and will be added in upcoming milestones.
 */

// Adapter exports will be added here as each platform is implemented.
// Example (once implemented):
// export { MagentoAdapter } from './magento/MagentoAdapter.js';
// export { ShopwareAdapter } from './shopware/ShopwareAdapter.js';
// export { ShopifyAdapter } from './shopify/ShopifyAdapter.js';
